# mouse-
⚠️ Screenshot Permission Required

Redesigned screenshot feature now supports two modes:

1️⃣ Accessibility Mode (Recommended):
   - Permission needed only once.
   - Go to: Settings > Accessibility > System Service and turn it ON.

2️⃣ Media Projection Mode (Standard):
   - Requires manual 'Start Now' click on device every time.
   - Attempting to request this now...